<div class="max-w-4xl mx-auto mt-8 bg-white p-6 rounded-lg shadow-md">
    <h1 class="text-2xl font-bold mb-6">Reporte: Servicios Más Vendidos</h1>
    <table class="table-auto border-collapse border border-gray-300 w-full">
        <thead>
            <tr class="bg-gray-200">
                <th class="border border-gray-300 px-4 py-2">Servicio</th>
                <th class="border border-gray-300 px-4 py-2">Cantidad Vendida</th>
                <th class="border border-gray-300 px-4 py-2">Ingresos Totales</th>
            </tr>
        </thead>
        <tbody>
            <% topServices.forEach(service => { %>
                <tr class="hover:bg-gray-100">
                    <td class="border border-gray-300 px-4 py-2"><%= service.service_name %></td>
                    <td class="border border-gray-300 px-4 py-2"><%= service.total_quantity %></td>
                    <td class="border border-gray-300 px-4 py-2">$<%= service.total_revenue %></td>
                </tr>
            <% }) %>
        </tbody>
    </table>
</div>
